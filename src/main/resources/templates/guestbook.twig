<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title></title>
    <link rel="stylesheet" href="../static/css/main.css">
    <link rel="stylesheet" href="../static/css/nav.css">
    <link rel="stylesheet" href="../static/css/guestbook.css">
</head>
<body>
    <div id="container">
        <nav id="mainNav">
            <span id="mainNavName">Menu</span>
            <ul id="mainNavList">
                <li><a href="guestbook">GuestBook</a></li>
                <li><a href="login">Login</a></li>
            </ul>
        </nav>
        <main id="mainContent">
            <ul id="comments">
                {% for comment in commentList %}
                    <li class="comment">
                        <div class="commentContent">
                            {{ comment.getCommentText() }}
                        </div>
                        <div class="commentProperties">
                            <div class="commentDate">{{ comment.getDateString() }}</div>
                            <div class="commentName">{{ comment.getLogin() }}</div>
                        </div>
                    </li>
                {% endfor %}
            </ul>
            <form id="commentForm" method="POST">
                <div id="leftFormPart">
                    <h3>Add your comment!</h3>
                    <input type="submit" id="commentButton" {{ commentButton }}>
                </div>
                <div id="rightFormPart">
                    <textarea id="commentContentInput" name="comment" placeholder="Your comment.." required {{ commentInput }}></textarea>
                </div>
                <h2 class="{{ commentInfo }}">You must be logged in to post comment!</h2>
            </form>
        </main>
        <footer>Site made by <a href="https://kamilbrodziak.github.io">Kamil Brodziak</a>. All rights reserved.</footer>
    </div>
</body>
</html>